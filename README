pipeline:

- run the redis database
- migrate the files {"ruby redis_adaptor.rb"}
- call the extractor file {./extractor.py [p/s [snapshot1 snapshot2]] > output}
  - p/s is perceptron/SVM, snapshot1 is the first snapshot, snapshot2 is the second snapshot (omit the .txt)
- call the analysis files {./analyze_libsvm.py trainingfile testfile > output}
- call the evalator file {./eval.py < data}
